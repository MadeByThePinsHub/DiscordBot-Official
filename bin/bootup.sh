# We use NPM to manage our packages and to save space in Glitch.

# Installs the package dependencies first, as listed in our package.json (unless you updated it)
npm install

# If installation are successful, run the bot.
node bot.js